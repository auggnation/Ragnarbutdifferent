# Mild-Viking Network Monitor — Python dependencies

# Web server
flask>=3.0.0
flask-socketio>=5.3.0
flask-cors>=4.0.0

# HTTP client (firewall API integration)
requests>=2.31.0
urllib3>=2.0.0

# System / network
psutil>=5.9.0
netifaces>=0.11.0

# Display / imaging (e-paper HAT)
Pillow>=10.0.0

# Terminal output
rich>=13.0.0

# Speed tests
speedtest-cli>=2.1.3

# Security (password hashing for web UI lock)
bcrypt>=4.0.0
cryptography>=41.0.0

# Data
pandas>=2.0.0

# Raspberry Pi hardware (ARM only — harmless to list on x86)
RPi.GPIO==0.7.1
spidev==3.5
smbus2>=0.4.0
